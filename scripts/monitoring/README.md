# Monitoring Scripts

These scripts provide simple infrastructure monitoring checks that can be used manually, with cron, or inside lightweight automation.

## Scripts

- `service-health-check.py`: Checks HTTP and TCP services from a JSON config file.
- `disk-space-monitor.sh`: Checks filesystem usage and exits with monitoring-friendly status codes.
