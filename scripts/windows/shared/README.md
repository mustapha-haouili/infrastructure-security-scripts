# Windows Shared Helpers

Shared PowerShell helper modules and reusable functions live here.

Use this folder for common report writers, JSON helpers, registry helpers,
admin checks, AD module checks, and safe confirmation helpers used by multiple
Windows scripts.

Do not place standalone admin-facing scripts here.
